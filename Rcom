options() #环境变量设置
lapply #对某一个表里面的每个元素做操作
grep #搜索函数，可用于大量数据文件中寻找特定数据; grepl和grep相似，只不过是返回所有行的结果，以TRUE和FALSE的方式呈现
table #统计各个因素出现的频率
read.table #导入某个文件作为表格，必要时候需要提供分隔符如sep = “\t”；comment.char一般默认为空格，可以改为其它符号，这个变量意义为注释语句起始符号
